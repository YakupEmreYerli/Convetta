/**
 * Dil önekinin iki değeri var: boş (Türkçe, `/`) ve `en` (`/en`). Opsiyonel
 * parametreleri prerender kendisi kesfedemedigi icin burada listeleniyor.
 */
export const entries = () => [{ lang: '' }, { lang: 'en' }];
